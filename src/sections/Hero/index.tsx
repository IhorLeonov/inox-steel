import { Container } from '@/components/Container';
import { Logo } from '@/components/common/Logo';

export const Hero = () => {
  return (
    <section>
      <Container>
        <Logo />
        Hero Section
      </Container>
    </section>
  );
};
